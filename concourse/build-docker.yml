platform: linux

image_resource:
  type: docker-image
  source:
    repository: docker

inputs:
- name: source-code

run:
  path: sh
  args:
  - -exc
  - |
    cd source-code
    docker-compose build
